% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastglmm.R
\name{fastglmm}
\alias{fastglmm}
\title{Fit a (generalized) linear or linear mixed model with the glmm Rust kernel}
\usage{
fastglmm(
  formula,
  data,
  family = gaussian(),
  weights = NULL,
  subset = NULL,
  na.action = getOption("na.action"),
  offset = NULL,
  nAGQ = 1L,
  start = NULL,
  wald.se = c("hessian", "rx"),
  dispersion = NULL,
  init.theta = NULL,
  ...
)
}
\arguments{
\item{formula}{an lme4-style model formula (or a string), e.g.
\code{y ~ t + d + t:d + (1 + t | g)}. See Details for what the shared parser
accepts beyond bare column names.}

\item{data}{a \code{data.frame} (or something coercible) holding every column
the formula names.}

\item{family}{a family object, family function, or string: one of
\code{gaussian}, \code{binomial} (logit/probit/cloglog), \code{poisson} (log), \code{Gamma}
(log/inverse), \code{inverse.gaussian} (log/\code{1/mu^2}), \code{"negativebinomial"}
(log; the shape \code{theta} is estimated).}

\item{weights}{optional per-row prior (case) weights - \code{lme4::glmer}'s
\verb{weights=}. For an aggregated binomial, pass the success \strong{proportion}
as the response and the trial count here, or equivalently write
\code{cbind(successes, failures)} as the formula's left-hand side; combining
both is an error.}

\item{subset}{optional row filter, evaluated in \code{data} like \code{lm}'s
\verb{subset=}. Applied before fitting - row filtering, not parsing.}

\item{na.action}{how to handle \code{NA}s in the model columns (default
\code{getOption("na.action")}, normally \link[stats:na.fail]{stats::na.omit}). \code{NA}s must be
resolved before the kernel sees the data; an action that leaves them in
place (\code{na.pass}) is an error.}

\item{offset}{optional per-row known additive term on the
linear-predictor scale, \code{glm}'s \verb{offset=}: \verb{eta = offset + X b (+ Z u)},
with no coefficient estimated for it. The usual use is a Poisson rate
model with a known exposure, \code{offset = log(exposure)}. Evaluated in
\code{data}, so an expression works; \verb{subset=} and \code{na.action} drop its
entries with the rows. Positioned after \code{na.action} as in \link[stats:glm]{stats::glm}.
Equivalent to an \code{offset()} term in the formula; combining both is an
error.}

\item{nAGQ}{adaptive Gauss-Hermite node count: an odd integer in
\verb{1..=25} (\code{1} = Laplace, the default). More permissive than lme4 in range
but see the fallback note in Details.}

\item{start}{optional warm start, lme4's name and shape:
\code{list(beta =, theta =)} with \code{theta} the random-effect Cholesky vector.
Distinct from \code{init.theta}, which is the negative-binomial shape.}

\item{wald.se}{Wald standard-error mode: \code{"hessian"} (default) or \code{"rx"}.}

\item{dispersion}{Gamma/inverse-Gaussian dispersion directive: \code{NULL}
(estimate via Pearson, the default), \code{"estimate"} (same), or a number to
hold it fixed. Non-\code{NULL} on binomial/poisson would mean
quasi-likelihood - GLMM 0.1.1, errors today.}

\item{init.theta}{negative-binomial shape seed, named for
\code{MASS::glm.nb(init.theta=)}. No kernel hook exists yet to seed the shape
search, so any non-\code{NULL} value is an error (the default cold start is
what runs).}

\item{...}{intercepted, never silently swallowed: known lme4 arguments
(\code{REML}, \code{control}, \code{verbose}, \code{contrasts}) raise errors saying why they
cannot be honored; unknown names error as unused arguments.}
}
\value{
An object of class \code{"fastglmm"}: fixed effects (\link{fixef}), Wald
covariance (\code{vcov()}), variance components on the SD/correlation scale
(\link{VarCorr}), \code{converged} and \code{singular} flags (\link{isSingular}), a
\code{diagnostics} list (the solver's own report: \code{boundary}, \code{pinned},
\code{notes}, plus the three flags above), plus
\code{print()}, \link[=summary.fastglmm]{summary()}, \link[=confint.fastglmm]{confint()}
(Wald), \code{nobs()}, \link[=formula.fastglmm]{formula()} (returns the formula
\strong{string}), \code{family()}, and \code{model.frame()}. Engine-blocked accessors
(\code{ranef}, \code{predict}, \code{fitted}, \code{residuals}, \code{coef}, \code{logLik}/\code{AIC},
\code{terms}) error with the reason.
}
\description{
One entry point for the whole \code{glmm} engine, dispatching on \code{family} the way
the kernel itself does: OLS (\code{gaussian} without random effects), GLM
(binomial/poisson/gamma/negative-binomial without random effects), REML LMM
(\code{gaussian} with random effects), and GLMM (Laplace or adaptive
Gauss-Hermite) - there is no \code{lmer}/\code{glmer} split.
}
\details{
The formula is parsed by the same Rust parser the Python port uses. Besides
bare column names, it accepts \code{log(x)}, \code{sqrt(x)}, \code{exp(x)}, and \code{I(x^k)}
(\code{k >= 2}) of a single column; \code{cbind(successes, failures)} on the
left-hand side with \code{family = binomial}; an \code{offset()} term; and term
removal (\code{- 1} / \verb{0 +}). Not accepted: \code{poly()}, arithmetic inside a call,
nested calls, and \code{.} - compute those columns first and pass them by name.
\verb{weights=} together with a \code{cbind()} formula, and \verb{offset=} together with
an \code{offset()} formula term, both error naming "use one". Fixed and random
effects support \code{+}, \code{:}, \code{*}, \code{A/B} nesting, and \code{(1 + x | g)} random-effect
terms with a \strong{full} correlation structure - \code{(x || g)} and intercept-free
RE terms are not fittable by the kernel and raise an error. Contrasts are
always treatment coding with the \strong{first factor level} as base; to change
the base, \code{relevel()} the factor (a \code{contrasts} argument is deliberately
absent). Character columns are converted to factors with lexicographic
level order (as \code{factor()} does); a factor's declared level order is
honored.

\strong{\code{Gamma()} link trap:} R's \code{Gamma()} family object defaults to
\code{link = "inverse"}, and a family \emph{object} is honored as given - R semantics
win. The string form \code{family = "gamma"} uses the glmm default
\code{link = "log"} instead. The two forms therefore fit different models;
choose deliberately.

\strong{\code{nAGQ} fallback (louder than lme4):} \code{nAGQ > 1} is honored on binomial
and Poisson mixed models with a single grouping factor and at most 3 random
effects per group. Any other shape \strong{warns and falls back to Laplace}
(\code{nAGQ = 1}) instead of erroring the way \code{lme4::glmer} does - the fit you
get is a Laplace fit, and the warning is the only notice. This mirrors the
Python port so the two ports agree.

\strong{Diagnostic warnings carry a condition class.} Each solver note is raised
as a warning of class \code{"fastglmm_diagnostic"} with a per-kind subclass
(\code{"fastglmm_ill_conditioned"}, \code{"fastglmm_pirls_exhausted"},
\code{"fastglmm_unused_grouping_levels"}, \code{"fastglmm_re_design_scale_spread"},
\code{"fastglmm_hessian_se_fallback"}; a note from a newer kernel than this
package arrives as \code{"fastglmm_unknown_note"}), so \code{withCallingHandlers()}
and \code{suppressWarnings(classes = )} can select them without matching message
text.

Anything the engine cannot do is an error naming the reason - never a
silently different model. That includes \code{REML = FALSE} (the LMM path is
REML-only \strong{by design}), \verb{control=}/\verb{verbose=}, and quasi-likelihood
\verb{dispersion=} on binomial/poisson.
}
\examples{
set.seed(1)
n_g <- 30L
g <- factor(rep(seq_len(n_g), each = 10))
t <- rep(seq(0, 0.9, by = 0.1), n_g)
d <- rbinom(n_g * 10L, 1L, 0.4)
u <- rnorm(n_g, sd = 0.8)[as.integer(g)]
y <- rbinom(n_g * 10L, 1L, plogis(-0.5 + 0.7 * t + 0.4 * d + u))
fit <- fastglmm(y ~ t + d + t:d + (1 | g), data.frame(y, t, d, g),
                family = binomial())
fixef(fit)
VarCorr(fit)
}
