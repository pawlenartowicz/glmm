% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastglmm-methods.R
\name{VarCorr}
\alias{VarCorr}
\alias{VarCorr.fastglmm}
\title{Variance components on the SD/correlation scale}
\usage{
VarCorr(x, ...)

\method{VarCorr}{fastglmm}(x, ...)
}
\arguments{
\item{x}{a \link{fastglmm} fit.}

\item{...}{unused.}
}
\value{
A list of class \code{"VarCorr.fastglmm"}, one named element per
grouping.
}
\description{
Returns one covariance matrix per grouping (declaration order), each with
\code{"stddev"} and \code{"correlation"} attributes, printed lme4-shaped. This is the
load-bearing accessor for parameter-recovery use: \code{attr(vc$g, "stddev")}
are the tau estimates and \code{attr(vc$g, "correlation")} the rho estimates,
straight from the kernel's \code{varcorr} (validated against lme4's \code{VarCorr}).
}
\details{
For a \strong{gaussian} mixed fit a \code{Residual} row is printed after the group
components, as in lme4, carrying \code{sigma()} (the REML residual standard
deviation the kernel reports as \code{dispersion}).
}
