# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.85"
name = "glmm"
version = "0.3.1"
build = false
publish = true
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Standalone f64 GLMM fit kernels (OLS, GLM, LMM, GLMM) in pure Rust on faer — the validation-pinned numerics from the MCPower engine."
readme = "README.md"
keywords = [
    "statistics",
    "glmm",
    "regression",
    "mixed-models",
    "mle",
]
categories = [
    "science",
    "mathematics",
]
license = "GPL-3.0-or-later"
repository = "https://github.com/pawlenartowicz/glmm"

[features]
alloc-tests = []
default = ["formula"]
formula = ["dep:regex"]
loop_advanced = []
oracle-tests = []
orchestrate = ["formula"]
parallel = ["dep:rayon"]

[lib]
name = "glmm"
crate-type = ["rlib"]
path = "src/lib.rs"

[[test]]
name = "contrasts_oracle"
path = "tests/contrasts_oracle.rs"

[[test]]
name = "formula_fit"
path = "tests/formula_fit.rs"

[[test]]
name = "lmm_ranef"
path = "tests/lmm_ranef.rs"

[[test]]
name = "parse_suite"
path = "tests/parse_suite.rs"

[[test]]
name = "validation_oracle"
path = "tests/validation_oracle.rs"

[dependencies.bobyqa]
version = "0.3"

[dependencies.pulp]
version = "=0.22.2"

[dependencies.regex]
version = "1"
optional = true

[dev-dependencies.dhat]
version = "0.3"

[dev-dependencies.serde]
version = "1"
features = ["derive"]

[dev-dependencies.serde_json]
version = "1"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies.faer]
version = "0.24"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies.rayon]
version = "1"
optional = true

[target.'cfg(target_arch = "wasm32")'.dependencies.faer]
version = "0.24"
features = [
    "std",
    "sparse-linalg",
]
default-features = false

[lints.clippy.all]
level = "deny"
priority = -1

[lints.rust]
unsafe_code = "warn"

[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = 0

[profile.test]
opt-level = 3
